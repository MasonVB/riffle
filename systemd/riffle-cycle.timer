[Unit]
Description=wake riffle four times a day

[Timer]
# Spread across the UTC day so it sees the board in different states, and
# never lands exactly on the 00:00 UTC cap reset.
OnCalendar=*-*-* 02,08,14,20:17:00 UTC
RandomizedDelaySec=900
Persistent=true

[Install]
WantedBy=timers.target
